# 2026-09-22 の回答を反映した仕様

ユーザーの回答により、従来の「仕様判断待ち12項目」は解消した。仕様の確定と実装・検証の完了は別に記録する。

| ID | 動作・範囲 |
| --- | --- |
| INTRO_013 | 指定秒数より短ければ曲末で停止。次曲を流さずループしない。 |
| INTRO_014 | 再生中の秒数変更は次回から適用。今回の表示・停止時刻は変えない。 |
| SESSION_009 | 再生ボタンの応答が届かない場合は、音を出さず状態を待機へ戻して再操作可能にする。内部の復旧処理として実装側で判断。 |
| SESSION_010 | 画面を閉じて開き直した場合は停止して待機へ戻す。 |
| SESSION_013 | 再読み込みでも解答者・得点・出題を復元し、進行を続けられる。イントロは停止して待機、準備は再実行、判定演出は完了させ、発表・結果は復元。 |
| LIBRARY_013 | 再生不能と判明した時点で除外。認証切れ・通信障害だけで再生不能とは判定しない。 |
| GAP_002 | バッファ待ちを秒数に含めず、音源が実際に進んだ時間で停止する。 |
| GAP_013 | ボタン以外から届いた再生状態では音を出さず待機へ復旧。内部処理として実装側で判断。 |
| SESSION_011 | 複数ホストは考慮不要との指示により対象外。 |
| SESSION_012 / SESSION_014 / UI_014 | サーバー再起動の永続復元・OS割り込み専用対応・判定訂正機能は今回追加しない。ユーザーの未回答タスクとして残さない。 |

物理スピーカーを使う実機確認は自動テストとは別。以前の574件の結果は今回の変更前の記録であり、新仕様の検証完了を意味しない。
